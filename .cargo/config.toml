[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# Rustc ko batao kitne threads use karo compilation mein
# (codegen-units se alag — ye frontend/macro expansion ke liye hai)
[build]
jobs = 8
